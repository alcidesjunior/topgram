<%= form_for(topfoto) do |f| %>
  <% if topfoto.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(topfoto.errors.count, "error") %> prohibited this topfoto from being saved:</h2>

      <ul>
      <% topfoto.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :descricao %>
    <%= f.text_field :descricao %>
  </div>

  <div class="field">
    <%= f.label :foto %>
    <%= f.file_field :foto %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
